# name: Python CI (INACTIVE - Uncomment to enable)
# on:
#   push:
#     branches: [ "main" ]
#   pull_request:
#     branches: [ "main" ]

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#     - uses: actions/checkout@v3
    
#     - name: Set up Python 3.10
#       uses: actions/setup-python@v3
#       with:
#         python-version: "3.10"
        
#     - name: Install dependencies
#       run: |
#         python -m pip install --upgrade pip
#         # Use the correct path for our monorepo structure
#         if [ -f backend/requirements.txt ]; then pip install -r backend/requirements.txt; fi
        
#     - name: Lint with flake8
#       run: |
#         pip install flake8
#         # The build will only stop on syntax errors or undefined names
#         if [ -d backend ]; then flake8 backend/ --count --select=E9,F63,F7,F82 --show-source --statistics; fi
